#include<stdio.h>
void main()
{
    int n,i,asum=0,esum,missing;
    printf("Enter the sixe of the array =");
    scanf("%d",&n);

    for(i=0,i<n,i++)
    {
        printf("Enter arr[%d]=",i);
        scanf("%d",&arr[i]);
    }

    for(i=0,i<n,i++)
        asum
    {
        printf("Enter arr[%d]=",i);
        scanf("%d",&arr[i]);
    }

    for(i=0,i<n,i++)
    esum = (n+1)/2;
    {
        printf("Enter arr[%d]=",i);
        scanf("%d",&arr[i]);
    }
    return 0;
}
